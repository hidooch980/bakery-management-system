# ممیزی سوم — وضعیت واقعی در برابر FINAL_SCOPE

تاریخ: ۱۴۰۵/۰۶/۱۵ (2026-09-05) · `main`، کامیت `ab6ca95` — **همان کامیتی
که همین حالا روی سرور اجرا می‌شود**

روش: همان روش دو ممیزی قبل — شمارش و grep روی مخزن واقعی، نه حدس. هر سه
سوئیت روی همین ماشین اجرا شد.

یک اشتباه در همین نوبت که ارزش نوشتن دارد: برای شمردن مدل‌های دارای
`RecordsAudit` اول `grep -rl "use RecordsAudit"` زدم و جواب **صفر** آمد.
سند می‌گفت ۲۳. اگر عدد سند را باور کرده بودم، اشتباه را نمی‌دیدم؛ اگر
عدد grep را باور کرده بودم، عددِ درست را خراب می‌کردم. علتش این بود که
تِرِیت‌ها روی یک خط جمع‌اند (`use BelongsToBakery, PostsToBankAccount,
RecordsAudit;`) و الگوی من فقط شکل تک‌خطی را می‌گرفت. با grep درست، ۲۳
تأیید شد. ابزار اندازه‌گیری هم به اندازهٔ چیزی که می‌سنجد جای اشتباه
دارد.

## چرا دوباره

ممیزی اول (۱۴۰۵/۰۶/۰۱، کامیت `8aa8c90`) نوشت که بند ۱۷ بیست درصد است و
«لاگ فعالیت سراسری نیست»، و بندهای ۱۰ و ۱۱ صفرند. هر سه در فاصلهٔ دو هفته
ساخته شدند و متن به‌روز نشد. نتیجه‌اش این بود که دو بار پشت سر هم کارِ
ساخته‌شده دوباره برنامه‌ریزی شد، چون سندی که تصمیم می‌گیرد بعدی چیست از خودِ
مخزن عقب مانده بود.

یک سند وضعیت که کهنه می‌شود، بدتر از نبودنش نیست — ولی از خودِ `git log`
هم بهتر نیست، و همین است که آن را بی‌فایده می‌کند. تاریخ بالای این فایل
بخشی از محتوای آن است.

## اندازهٔ چیزی که ساخته شده

| | اول | دوم | امروز |
|---|---|---|---|
| مدل‌های Eloquent | ۴۰ | ۴۸ | **۴۸** |
| مهاجرت | ۱۰۵ | ۱۱۸ | **۱۱۸** |
| Resource در پنل | ۲۹ | ۳۲ | **۳۲** |
| صفحه و ویجت پنل | ۸ + ۲۱ | ۱۱ + ۲۱ | **۱۱ + ۲۱** |
| فایل تست Feature | ۱۰۲ | ۱۳۹ | **۱۴۶** |
| **تست سبز (بک‌اند)** | ۱۰۹۱ | ۱۴۶۶ | **۱۴۸۱** |
| **تست سبز (موبایل)** | — | ۳۶۱ | **۳۷۹** |
| **تست اسکریپت استقرار** | — | — | **۲۴** |
| فایل Dart | ۷۵ | ۸۸ | **۹۶** |

هر سه عدد از اجرای امروز است: بک‌اند ۱۴۸۱ تست / ۴۴۵۸ assertion در ۲۶۲
ثانیه روی MySQL، موبایل ۳۷۹ تست با `flutter analyze` تمیز، و ۲۴ بررسی
روی `scripts/deploy.sh`.

هیچ مهاجرت تازه‌ای از ممیزی دوم اضافه نشده. کار این دو روز روی چیزهایی
بوده که از قبل بودند و درست کار نمی‌کردند.

## جدول پیشرفت

| # | بند FINAL_SCOPE | اول | دوم | امروز | شواهد |
|---|---|---|---|---|---|
| ۱ | Full Audit + Error Fix | ۶۰٪ | ۶۰٪ | **۷۰٪** | `IssueScanner`, `SystemIssue`, `stock:audit`. از امروز هر ردیف «امروز» روی گوشی باز می‌شود و علت و راه‌حل و **دکمهٔ رفتن به همان تب** دارد؛ `IssueDestination` مقصد را سمت سرور تعیین می‌کند. تشخیص، همان تشخیص است؛ آنچه اضافه شد رساندنِ آدم به جایی است که کار را می‌کند |
| ۲ | Multi-Bakery | ۸۵٪ | ۸۵٪ | ۸۵٪ | `Bakery`, `BelongsToBakery`, `bakery:create` — فعال‌سازی هنوز تصمیم مالک است |
| ۳ | جداسازی دادهٔ نانوایی‌ها | ۸۰٪ | ۸۰٪ | ۸۰٪ | `MultipleBakeriesTest` سبز |
| ۴ | امنیت کاربر و دستگاه | ۷۰٪ | ۸۵٪ | **۸۵٪** | RBAC، `throttle`، `PruneIdleTokens`، بازیابی با پیامک، `local_auth`، و از امروز فهرست دستگاه‌ها با نام هر گوشی و خروج تکی یا گروهی (`GET /devices`, `DELETE /devices/{token}`, `DELETE /devices/others`) |
| ۵ | عملکرد واقعی کارکنان | ۴۰٪ | ۴۰٪ | ۴۰٪ | کیفیت/ضایعات/هدف‌در‌برابر‌واقعی همچنان نیست |
| ۶ | گزارش وظیفه‌محور | ۳۵٪ | ۳۵٪ | ۳۵٪ | هیچ مدل `Task`ی وجود ندارد؛ خمیر و چانه per-person هست |
| ۷ | شفافیت مالی فروشنده | ۸۰٪ | ۸۰٪ | ۸۰٪ | `SellerSettlement`, `SettlementRequest`, بدهی مشتری |
| ۸ | حقوق، علی‌الحساب، وام، دفتر کارمند | ۸۵٪ | ۸۵٪ | ۸۵٪ | کامل‌ترین بخش |
| ۹ | تطبیق خمیر تا فروش | ۶۵٪ | ۶۵٪ | ۶۵٪ | `ProductionRecorder`, `SystemVersusOven`, `stock:audit` |
| ۱۰ | Offline-First | ۰٪ | ۹۰٪ | **۸۵٪** | `OfflineQueue` روی SQLCipher، `ResponseCache`، `ConnectionStatus`، ۱۶ اندپوینت نوشتن. **عدد پایین آمد، چون ادعای دیروز روی گوشی واقعی غلط بود** — پایین‌تر. حالا: پوشهٔ پایگاه ساخته می‌شود، صف اگر پایگاه باز نشود به `SecureStore` برمی‌گردد، و «امروز» و فرم هزینه هم از کش می‌خوانند |
| ۱۱ | همگام‌سازی + حل تعارض | ۰٪ | ۷۰٪ | **۷۰٪** | `Idempotency-Key` روی هر تلاش، بازپخش به ترتیب، فهرست «ردشده‌ها» با دلیل سرور، `IdempotentWrites` روی کل گروه احراز هویت |
| ۱۲ | ثبت صوتی فارسی | ۰٪ | ۰٪ | **۰٪** | هیچ پکیج STT در `pubspec.yaml` نیست |
| ۱۳ | گزارش‌های هوشمند | ۷۰٪ | ۷۰٪ | **۷۵٪** | `BalanceSheet`, `Ledger`, `ProfitAndLoss`, ۲۱ ویجت. گوشی از امروز «تفکیک فروش» (به تفکیک نوع پرداخت و فروشنده)، «تولید» و «مصرف» دارد، و نمودار درآمد می‌گوید پول از نان آمده یا از آرد. هیچ اندپوینت تازه‌ای لازم نشد — هر سه از قبل بودند و گوشی نمی‌پرسیدشان؛ `productionReport()` حتی تعریف شده بود و هیچ‌جا صدا زده نمی‌شد |
| ۱۴ | پنل سه‌بعدی | ۰٪ | ۰٪ | **۰٪** | هیچ پکیج گرافیکی نیست |
| ۱۵ | مغز دوم نانوایی | ۰٪ | ۰٪ | **۰٪** | — |
| ۱۶ | تحلیل و پیش‌بینی | ۱۵٪ | ۱۵٪ | **۳۰٪** | `Outlook` روی «امروز»: آرد چند روز کافی است، سهمیهٔ دوره تا آخر می‌رسد یا نه، سود پایان دوره با روند فعلی — هر سه با مبنایشان روی صفحه (میانگین ۱۴ روز اخیر) و **بدون سابقهٔ کافی هیچ خطی نمی‌آید**. حساب نرخ روند است، نه مدل؛ همین‌طور هم نام‌گذاری شده. پیش‌بینی به معنای یادگیری همچنان نیست |
| ۱۷ | Audit Trail | ۲۰٪ | ۸۵٪ | **۸۵٪** | `AuditLog`, `RecordsAudit`, `AuditLogResource`. ۲۳ مدل ثبت می‌کنند: هم آن‌ها که پول را جابه‌جا می‌کنند، هم آن‌ها که تعیین می‌کنند چقدر است |
| ۱۸ | کارایی | ۴۰٪ | ۷۵٪ | **۸۰٪** | شش N+1 بسته شد. `/today` ۱۰۹→۶۵→۷۰ (پیش‌بینی، هزینهٔ ثابت)، `seller-accounts` از خطی به ثابت، گزارش‌های پنل **۶۰۳→۹۲**، `shop-today`/`issue-center` **۴۰۴→۲۸**، و از امروز `/reports/production` که بازه را روز‌به‌روز راه می‌رفت (**۲۴۰ کوئری برای سه ماه**) و `/reports/sales` که برای نام هر فروشنده یک `find` می‌زد. سه تست رشد، هر کدام یک اندازه در برابر ده. سنجیده‌نشده: زمان زیر بار واقعی، نه تعداد کوئری |
| ۱۹ | تست و امنیت و رگرسیون | ۶۵٪ | ۸۰٪ | **۸۰٪** | ۱۴۸۱ + ۳۷۹ + ۲۴ تست. ممیزی امنیتی API یک آسیب‌پذیری (نشستِ حساب غیرفعال) بست. اسکریپت استقرار از امروز تست دارد. **زدن از بیرون با ابزار همچنان نشده** — و این نوبت نشان داد که تستِ سبز جای گوشی واقعی را نمی‌گیرد |
| ۲۰ | آمادگی تولید | ۷۰٪ | ۷۰٪ | **۷۵٪** | زنده، پشتیبان دولایه. `deploy.sh` از امروز مهاجرت معلق را پشت `artisan down` اجرا می‌کند و اگر شکست بخورد کد را برمی‌گرداند — پیش از این فقط چاپش می‌کرد و رد می‌شد؛ ۲۴ تست دارد. استقرار از گیت‌هاب نوشته شده ولی **هنوز اجرا نشده** (پایین‌تر). **HTTPS همچنان ندارد** |
| ۲۱ | مدیریت خودگردان و کم‌هزینه | ۱۰٪ | ۱۰٪ | ۱۰٪ | فایل‌های State این بسته هنوز ساخته نشده‌اند |

### جمع‌بندی: **حدود ۶۵٪** (دوم: ۶۲٪ · اول: ۴۷٪)

دو واحد در دو روز، و این عدد کوچک، خودش حرف اصلی این ممیزی است.

هیچ قابلیت تازه‌ای ساخته نشد. دو روز صرف این شد که چیزهایی که سند
می‌گفت **ساخته شده‌اند** واقعاً روی گوشیِ مالک کار کنند — و یکی‌شان
اصلاً کار نمی‌کرد. بند ۱۰ حتی **پایین آمد**، از ۹۰ به ۸۵، چون ادعای
دیروز درست نبود.

- **آنچه مغازه امروز با آن کار می‌کند** (۱ تا ۹ و ۱۳ و ۱۹ و ۲۰): حدود
  **۷۳٪**. تنها بخشی که واقعاً جلو رفت، و از راه درست‌کردن رفت نه
  اضافه‌کردن.
- **آنچه اصلاً شروع نشده** (۱۲، ۱۴، ۱۵): همچنان **صفر**. `pubspec.yaml`
  هیچ پکیج STT و هیچ پکیج گرافیکی ندارد؛ امروز دوباره شمرده شد.

درسی که این نوبت گرفت و ارزش نوشتن دارد: **«تست سبز است» و «روی گوشی
کار می‌کند» دو ادعای متفاوت‌اند.** بند ۱۰ با ۳۶۱ تست سبز، ۹۰٪ ثبت شد و
روی اولین نصب واقعی هیچ‌وقت باز نشد.

## یافته‌های CRITICAL / HIGH

### ~~CRITICAL — دیتابیس محلی روی نصب تازه اصلاً باز نمی‌شد~~ → بسته شد

این یافتهٔ اصلی این ممیزی است، و ممیزی قبل باید پیدایش می‌کرد و نکرد.

سند دیروز نوشت «**دیتابیس محلی نشست**» و بند ۱۰ را ۹۰٪ زد. کد درست بود،
۳۶۱ تست سبز بود، و روی اولین گوشیِ واقعی فایل **هرگز باز نشد**:

```
DatabaseException(open_failed /data/user/0/…/databases/bakery_local.db)
```

اندروید پوشهٔ `databases/` را خودش نمی‌سازد. باز کردن فایل داخل پوشه‌ای
که نیست با همین یک جمله شکست می‌خورد و بس. هیچ تستی نگرفتش چون همه روی
دیتابیسِ در حافظه اجرا می‌شدند، جایی که پوشه‌ای نیست که نباشد.

سه پیامد داشت، و هر کدام از دیگری بدتر:

۱. **صف نمی‌توانست یک فروش را نگه دارد** — تنها کاری که این برنامه هرگز
   نباید در آن شکست بخورد. رفتارش هم بی‌صدا بود: `enqueue` داخل بلوک
   `catch` صدا زده می‌شد، پس خطایش از دستِ صفحه — که فقط `ApiException`
   می‌گیرد — بیرون می‌زد. نه پیام موفق، نه پیام خطا. (این با یک برنامهٔ
   کوچک Dart جداگانه آزموده شد، نه از روی خواندن کد.) تنها نشانه این بود
   که چانه در فهرست «در انتظار» می‌ماند.
۲. **کش خواندن هم می‌شکست** و چون `save` — برخلاف `read` — محافظت نداشت،
   یک درخواستِ **موفق** موقع ذخیرهٔ نسخه‌اش خطا می‌داد. صفحهٔ «امروز»
   مالک، با آنتن کامل، جعبهٔ قرمز خطا شد.
۳. **مالک این را گزارش کرد، نه ما.**

هر سه بسته شد: پوشه پیش از باز کردن ساخته می‌شود؛ `save` و `clear`
best-effort شدند؛ و صف اگر پایگاه باز نشود به همان لیست JSON در
`SecureStore` برمی‌گردد — رمزگذاری‌شده، کندتر، درست — و عمداً زیر کلیدهای
قدیمی می‌نویسد تا کدِ انتقالِ موجود، اولین باری که پایگاه باز شد، خودش
آن‌ها را به جدول‌ها ببرد. شش تست، که **پنج‌تایشان روی نسخهٔ قبلی رد
می‌شوند**.

**دادهٔ از دست رفته:** پنجرهٔ خطر از ۱۳:۱۳ تا ۲۳:۰۰ همان روز بود (نسخهٔ
۴.۸۸ تا ۴.۸۹) و فقط ثبتِ **بدون اینترنت** را می‌گرفت؛ مسیر آنلاین اصلاً
به پایگاه محلی دست نمی‌زند. مالک فروش‌های آن روز را مرور کرد: همه با
اینترنت بوده. چیزی گم نشد.

### CRITICAL — بدون HTTPS، رمز عبور روی سیم ساده می‌رود
هنوز باز، ولی دیگر بلوکه نیست. نام آمد: `baker.molido.ir`.

کانفیگ TLS نوشته و **آزموده** شد — روی nginx واقعی بالا آمد و با گواهی
آزمایشی TLS 1.3 و HTTP/2 داد؛ ریدایرکت پورت ۸۰ کار کرد و مسیر
`/.well-known/acme-challenge/` از ریدایرکت مستثنا ماند. runbook گام‌به‌گام
در `deploy/README.md`.

**DNS درست شد.** پروکسی آروان خاموش شد و `baker.molido.ir` حالا مستقیم به
`37.32.21.125` می‌رود. (پیش از آن به anycast ابرآروان می‌رفت و اثبات از راه
فایل به سرور ما نمی‌رسید.)

آنچه مانده اجرای runbook روی خود سرور است — دسترسی به آن ماشین از این‌جا
وجود ندارد. یک پیش‌نیاز دیگر هم هست که ارزش دارد **قبل** از certbot آزموده
شود: سرورهای اعتبارسنجی Let's Encrypt در آمریکا و اروپا هستند، و اگر
فایروالِ سرور یا دیتاسنتر ترافیک خارجی را ببندد، اثبات با «Timeout during
connect» شکست می‌خورد — پیامی که دقیقاً شبیه DNS غلط خوانده می‌شود در حالی
که DNS سالم است. آزمونش و راه دور زدنش (اثبات از راه رکورد TXT) در
`deploy/README.md`.

سه تصمیم که در کانفیگ توضیح داده شده و ارزش دانستن دارد:

- **TLS در فایل جدا است.** nginx وقتی `listen ... ssl` به گواهی ناموجود
  اشاره کند اصلاً بالا نمی‌آید — یعنی مغازه تعطیل. پس فایل TLS تازه بعد
  از صدور گواهی نصب می‌شود.
- **بلوکِ IP دست‌نخورده می‌ماند.** گوشی‌های نصب‌شده با `37.32.21.125` روی
  ۸۰ و ۸۰۰۰ حرف می‌زنند. ریدایرکت فقط روی نام است؛ ریدایرکت سراسری
  پورت ۸۰ یعنی هیچ‌کس نمی‌تواند نان بفروشد.
- **HSTS فعلاً نه.** پس گرفتنش با ویرایش فایل روی سرور ممکن نیست؛ دستور
  از قبل داخل گوشی است. بعد از یک تمدید موفق و یک هفته کار.

### ~~HIGH — نبودِ Offline~~ → کاهش یافت به MEDIUM
۱۸ مرداد سیستم قطع شد و یک روز کاملِ پخت و فروش ثبت نشد. امروز آن روز
گم نمی‌شد: صف، نوشتن را نگه می‌دارد و با هر بار وصل‌شدن با همان
`Idempotency-Key` می‌فرستد، و آنچه سرور رد کند در فهرست «ردشده‌ها» با
دلیل خودش می‌ماند تا کسی ببیندش.

از امروز، نسخهٔ ذخیره‌شده روی صفحه اعلام می‌کند که ذخیره‌شده است، با
ساعتی که گرفته شده. پیش از این `servedFrom` وجود داشت و هیچ صفحه‌ای
صدایش نمی‌زد: مانده‌حساب بانکی دیشب، ظهر، دقیقاً به شکل امروز دیده می‌شد.

پنجرهٔ دوازده‌ساعته هم برداشته شد. کپیِ کهنه‌تر از آن، پیش‌تر اصلاً سرو
نمی‌شد — مغازه‌ای که موقع بستن سیگنال را از دست می‌داد، صبح روز بعد خطا
می‌گرفت در حالی که ارقام دیروز همان‌جا در حافظه نشسته بود. حالا سرو
می‌شود و بنر می‌گوید «کهنه‌اند»؛ آنچه آن پنجره می‌خواست جلویش را بگیرد،
با گفتنِ سنِ رقم ارزان‌تر گرفته می‌شود تا با پنهان‌کردنش.

**دیتابیس محلی شروع شد.** `LocalDatabase` روی SQLCipher نشست و صف نوشتن
اولین چیزی است که به آن منتقل شد. پیش از این کل صف یک رشتهٔ JSON در
`SecureStore` بود: هر ثبت، همه‌اش را بازنویسی می‌کرد، و «چند تا در انتظار
است» — که صفحهٔ خانه مدام می‌پرسد — همه‌اش را دوباره پارس می‌کرد. سرِ سه
ردیف درست است و سرِ سیصد تا نه، و سیصد تا یعنی یک روز بی‌شبکه.

رمزنگاری اختیاری نبود: چیزی که آنجاست فروش‌های نرسیده است، با مبلغ و
مشتری و توضیح — همان که `SecureStore` برایش نوشته شد («گوشی‌ای که در تاکسی
جا می‌ماند باید فقط یک گوشی خرج بردارد»). فایل sqlite ساده روی گوشی روت‌شده
خواندنی است، پس انتقال به آن یعنی خنثی‌کردن دلیلی که این داده اول از همه
جابه‌جا شد. کلید یک بار ساخته و در `flutter_secure_storage` می‌ماند.

کش خواندن هم منتقل شد، و یک نشتی همان‌جا بسته شد: **هیچ چیز هرگز یک
ورودی را حذف نمی‌کرد.** هر گزارشی که مدیر باز می‌کرد تا آخر عمرِ نصب روی
گوشی می‌ماند و فقط خروج از حساب پاکش می‌کرد — کشی که فقط بزرگ می‌شود
نشتی است با اسم دوستانه. حالا سقف دارد و کهنه‌ترین‌ها می‌افتند.

**و بعد معلوم شد هیچ‌کدام از این‌ها روی گوشی باز نمی‌شد.** بخش CRITICAL
بالا. آنچه اینجا نوشته شده دربارهٔ کد درست بود و دربارهٔ دستگاه نبود، و
فاصلهٔ آن دو یک روز کار مالک را گرفت.

دو چیز دیگر هم امروز بسته شد که از جنس همین‌اند: صفحهٔ «امروز» و فهرست
دسته‌های هزینه مستقیم از سرور می‌پرسیدند و از کش رد نمی‌شدند — پس در
حالت هواپیما، صفحهٔ اولِ مالک جعبهٔ قرمز خطا بود و ثبت هزینهٔ آفلاین
اصلاً ممکن نبود، چون فرم بدون دسته ذخیره نمی‌کند. صف سالم بود؛ پشت دری
بود که باز نمی‌شد.

آنچه هنوز مانده: این یک انبارِ سطر‌به‌سطر نیست، هنوز یک پاسخ کامل به‌ازای
هر مسیر است. برای «دفتر دیروز را باز کن» کافی است؛ برای «فروش‌های این ماه
را آفلاین فیلتر کن» نه.

و یک چیز که هیچ‌کس هنوز انجام نداده: **آزمایش حالت هواپیما روی گوشی
واقعی با نسخهٔ ۴.۸۹**. هر سه اصلاح بالا فقط تست خودکار دارند، و این
ممیزی تازه نشان داد که آن کافی نیست.

### ~~HIGH — Audit Trail سراسری نیست~~ → بسته شد
۲۳ مدل حالا `RecordsAudit` دارند و هر تغییر با نامِ تغییردهنده، مقدار پیش
و پس، و IP نوشته می‌شود. `auditSubject()` هر رکورد خودش را به کلمه صدا
می‌زند، چون لاگ از رکورد بیشتر عمر می‌کند و id به چیزی اشاره نمی‌کند که
پاک شده.

باقی‌ماندهٔ این بند دفترهای مشتق است — تراکنش بانکی، حرکت انبار و آرد،
بازیافت‌های حقوق — که عمداً بیرونند: آن‌ها را همان مدل‌های بالا می‌نویسند،
نه یک آدم، و ثبتشان یعنی یک ویرایش دو بار زیر ردیفی که کسی تایپش نکرده.

### ~~MEDIUM — باطل‌کردن دستگاه~~ → بسته شد
سه راهی که نشست را می‌بستند همه پیامد جانبی کار دیگری بودند، و آن که
واقعاً استفاده می‌شد — غیرفعال‌کردن کاربر — قیمتش این بود که همان آدم تا
اطلاع ثانوی نمی‌توانست کار کند.

حالا «دستگاه‌های من» در تنظیمات هست: هر گوشی با نام خودش (`device_info_plus`
هنگام ورود می‌فرستد)، «همین گوشی» را سرور علامت می‌زند نه اپ، و خروج یا
تکی است یا «بقیه به‌جز این یکی». شناسهٔ متعلق به حساب دیگر ۴۰۴ می‌گیرد،
نه ۴۰۳ — و مهم‌تر اینکه بسته نمی‌شود.

### ~~MEDIUM — کارایی، بازبینی سیستماتیک~~ → دور اول انجام شد
روشش این بود: هر اندپوینت و هر صفحهٔ پنل **دو بار** پرسیده شد، یک بار با
چند رکورد و یک بار با ده برابر، و اختلاف گزارش شد — چون N+1 سرِ یک ردیف
نامرئی است.

سه مورد پیدا و بسته شد:

- **`/api/v1/today`: ۱۰۹ کوئری** و با شلوغی بالا می‌رفت. این صفحه‌ای است
  که مالک اول از همه باز می‌کند و اپ بدون کش می‌گیردش. ۴۶ تای آن یک
  `sum()` تکراری روی دفتر انبار بود که در یک درخواست بیست‌وسه بار خوانده
  می‌شد. حالا **۶۵ و مسطح** — و ۷۰ با پیش‌بینی، که هزینهٔ ثابت است نه
  به‌ازای هر ردیف: نرخ مصرف یک جمعِ گروه‌بندی‌شده به تفکیک روز است.
- **صفحهٔ گزارش‌های پنل: ۶۰۳ کوئری** در یک بار باز شدن — هر شاخص برای هر
  روز کوئری خودش را داشت. حالا هر شاخص یک کوئریِ گروه‌بندی‌شده به تفکیک
  روز است و سطل‌ها در PHP جمع می‌شوند؛ گروه‌بندی به **روز** و نه به سطل،
  چون سطل‌ها هفته و ماه جلالی‌اند و SQL آن تقویم را ندارد. **۶۰۳ → ۹۲**،
  در دو دور: اول `financial` و `production`، بعد `consumption`.
- **`seller-accounts`** به‌ازای هر فروشنده یک کوئری داشت — یعنی با فهرست
  کارکنان رشد می‌کرد. حالا یکی برای همه.
- **`shop-today` و `issue-center`** (هر دو روی یک `IssueScanner`): با یک
  فروشنده/حساب/وام **۹۸ کوئری، با ده تا ۴۰۴**. بزرگ‌ترین سهم نه از این دو
  صفحه که از زیرِ همه بود: `CurrentBakery` وقتی کسی وارد نشده (کنسول،
  زمان‌بند، صف) شعبه را کش نمی‌کرد و هر global scope دوباره می‌پرسید —
  دویست بار در یک اسکن. بعدش `paid` وام و `balance` حساب که در هر خواندن
  یک `sum()` می‌زدند (هشت بار برای هر وام)، و دو چکِ فروشنده که یک کوئری
  را سه بار برای هر نفر تکرار می‌کردند. حالا **۲۸ و مسطح**؛ تستی هست که
  یک و ده را می‌سنجد و اختلاف را خطا می‌گیرد.

- **`/reports/production`** بازه را روز‌به‌روز راه می‌رفت و برای هر روز
  دو `->get()` می‌زد: **۲۴۰ کوئری برای سه ماه**، با بارگذاری هر ردیف خمیر
  و چانه فقط برای جمع‌زدن چهار ستون. و **`/reports/sales`** برای نامِ هر
  فروشنده یک `User::find` داخل گروه‌بندی می‌زد — روی داده‌ای که همان لحظه
  کامل در حافظه بود. هر دو حالا مسطح، با تستی که یک اندازه و ده را
  می‌سنجد.

  این دو را وقتی پیدا کردم که برای نشان‌دادنشان روی گوشی سراغشان رفتم.
  هیچ‌کدام در بازبینی «سیستماتیک» دور اول نیامده بودند، چون آن دور
  اندپوینت‌هایی را سنجید که اپ **صدا می‌زد** — و این دو را صدا نمی‌زد.
  بازبینیِ سیستماتیک هم مرز دارد، و مرزش همان‌جاست که کسی نگاه نکرده.

ارقام **قبل** از دست‌زدن به کد قفل شدند: هر سطل، در هر سه دانه‌بندی، در
برابر جمعِ کند و آشکار همان بازه. این کد پول گزارش می‌کند؛ صفحه‌ای که
سریع شود و رقمش عوض شود از صفحهٔ کند بدتر است.

آنچه سنجیده **نشده**: زمان زیر بار واقعی، نه تعداد کوئری. صفحه‌ای که با
اندازهٔ مغازه رشد کند، در حد آنچه اندازه گرفته شد، دیگر نمانده.

### ~~MEDIUM — استقرار یعنی یک نفر ترمینال باز کند~~ → نیمه‌بسته

`deploy.sh` مهاجرت‌های معلق را **چاپ می‌کرد و رد می‌شد**. بدترین کاری که
می‌شد با آن‌ها کرد: کد تازه روی ساختار قدیمی زنده می‌شد و سایت همچنان
۲۰۰ می‌داد، پس هیچ‌جا نمی‌گفت مغازه خراب است. همان شکلِ چهار روزِ پشت
صفحهٔ سبز که «امروز» برای آن ساخته شد — بررسی‌ای که نگاه می‌کند و با
چیزی که دید کاری نمی‌کند.

حالا اجرایشان می‌کند، آن هم فقط وقتی هست: نسخهٔ بدون مهاجرت مغازه را
اصلاً نمی‌بندد. با مهاجرت، پشت `artisan down` — چون بین مهاجرت و ری‌لود
هنوز کدِ **قدیم** سرو می‌شود. مهاجرتی که شکست بخورد کد را به کامیت قبلی
برمی‌گرداند. `artisan up` روی trap است، پس هیچ خطایی نمی‌تواند نانوایی را
بسته رها کند. و بررسی سلامتِ آخر حالا کد خروج را تعیین می‌کند — پیش از
این چه مغازه بالا می‌آمد چه نه، «موفق» برمی‌گرداند.

۲۴ بررسی در هفت سناریو، با استاب‌کردن `git`/`php`/`sudo`/`curl`؛ **۱۶
تایشان روی نسخهٔ قبلی رد می‌شوند**. فایلی که اولین اجرای واقعی‌اش روی یک
مغازهٔ زنده است نباید اولین جای پیدا شدن اشتباهاتش هم باشد.

**نیمهٔ باز:** استقرار از گیت‌هاب (`Actions → Deploy`) نوشته و تست شده
ولی **یک بار هم اجرا نشده** — گیت‌هاب اجرا را با `action_required` و صفر
job متوقف می‌کند و علتش از بیرون دیده نمی‌شود. پنج تلاش، سه‌تای آخر همه
پیش از شروع متوقف. تا روشن شدن آن، استقرار همان دو خط دستی است که کار
می‌کند.

یک باگ در پیش‌نویس اول خودم که ارزش ثبت دارد: workflow ورودی `ref` داشت و
مستندات با آن وعدهٔ برگرداندن نسخه می‌داد، در حالی که `deploy.sh` در دلش
`git pull origin main` ثابت داشت. زدن `v4.88.0` بی‌صدا همان نسخهٔ خرابی را
دوباره مستقر می‌کرد که قرار بود از آن عقب بنشینیم. تصمیمِ «چه چیزی
checkout شود» در دو جا بود، و این‌طور است که دو جا با هم اختلاف پیدا
می‌کنند.

### ~~MEDIUM — تست نفوذ نشده~~ → دور اول انجام شد
یک ممیزی امنیتی روی سطح API انجام شد: هر ۵۶ مسیرِ پارامتردار در برابر
گیت دسترسی‌شان، مالکیت رکورد در اندپوینت‌های «مالِ خودم»، جداسازی
نانوایی‌ها، SQL خام، و آنچه در پاسخ‌ها بیرون می‌رود.

**یک آسیب‌پذیری واقعی پیدا و بسته شد:** `is_active` فقط در یک نقطه خوانده
می‌شد — هنگام ورود. توکن از تصمیمی که آن را ساخته بیشتر عمر می‌کند، پس
حسابی که غیرفعال شده بود روی گوشیِ در دست، همچنان فروش ثبت می‌کرد و حقوق
می‌خواند. تنها مسیری که تمیز می‌کرد اکشن toggle پنل بود؛ فرم ویرایش همان
ستون، دستور کنسول، و اصلاح مستقیم در دیتابیس هیچ‌کدام نه. حالا میان‌افزار
`EndsInactiveSessions` روی کل گروه احراز هویت این را در **در ورودی** می‌بندد
و توکن‌ها را پاک می‌کند، نه اینکه درخواست‌ها را یکی‌یکی رد کند.

**یک یافتهٔ جانبی:** بازنشانی رمز یک کاربر توسط مدیر، نشست‌های باز با رمز
قدیمی را نمی‌بست — یعنی نیمهٔ اشتباهِ مسئله‌ای که معمولاً دلیل بازنشانی
است. حالا می‌بندد.

**چیزی که سالم بود:** گیت دسترسی روی همهٔ مسیرها به‌جز سه مورد «مالِ
خودم» که هر سه مالکیت را خودشان بررسی می‌کنند؛ `BelongsToBakery` به‌عنوان
scope سراسری، پس نشتی بین نانوایی‌ها نبود؛ SQL خام در هر پنج مورد یا ثابت
است یا با placeholder؛ `$hidden` هش رمز را بیرون نمی‌دهد؛ خروجی PowerBI
allow-list دارد.

**این تست نفوذ نیست، ممیزی کد است.** هیچ‌کس این سیستم را از بیرون و با
ابزار نزده. بندهایی که یک ابزار پیدا می‌کند و این ممیزی نمی‌کند — پیکربندی
سرور، نسخهٔ کتابخانه‌ها، رفتار زیر بار — همچنان باز است، و بیشترشان تا
وقتی HTTPS نیست بی‌معنی‌اند.

## ترتیب پیشنهادی

۰. **آزمایش حالت هواپیما روی گوشی، با ۴.۸۹** — ارزان‌ترین کار این فهرست و
   حالا اولِ آن، چون تنها چیزی است که این ممیزی ثابت کرد جایگزین ندارد.
   حالت هواپیما، یک ثبت، برگرداندن اینترنت، دیدن اینکه رسید. کار مالک.
۱. **HTTPS** (بند ۲۰) — تنها CRITICAL باقی‌مانده و ارزان‌ترین: یک دامنه و
   یک `certbot`. کار مالک. `baker.molido.ir` آماده است.
۲. **باز کردن استقرار از گیت‌هاب** — نوشته و تست شده، فقط اجازهٔ اجرا
   ندارد. یک نگاه به صفحهٔ اجرا کافی است.
۳. **تست نفوذ واقعی** (بند ۱۹) — ممیزی کد انجام شد و یک آسیب‌پذیری بست.
   آنچه نشده، زدنِ سیستم از بیرون با ابزار است؛ و تا وقتی HTTPS نیست،
   بیشترِ آن بی‌معنی است.

بندهای ۱۲ (صوت)، ۱۴ (سه‌بعدی) و ۱۵ (مغز دوم) همچنان عمداً آخر: هیچ‌کدام
دادهٔ غلط را درست نمی‌کنند و هر سه روی زیرساختی سوارند که هنوز کامل نیست.

---

ممیزی‌های پیشین در تاریخچهٔ گیت‌اند:

- اول: `git show 8aa8c90:docs/FINAL_AUDIT_REPORT.md`
- دوم: `git show 203fe85:docs/FINAL_AUDIT_REPORT.md`

هر سه نوبت یک الگو داشتند: سند از مخزن عقب افتاد. نوبت اول دو هفته، نوبت
دوم یک روز — و نوبت دوم بدتر بود، چون سند نمی‌گفت کار **نشده**، می‌گفت
کاری که روی گوشی از کار افتاده بود ۹۰٪ آماده است. عددی که بالا برود
راحت‌تر از عددی که پایین بیاید نوشته می‌شود، و همین است که سند را
بی‌اعتبار می‌کند.
