# این مخزن فارسی نوشته می‌شود

صاحب مغازه فارسی می‌خواند. هر چیزی که ممکن است کسی بخواندش، فارسی است.

## قاعده

**هر خطِ تازه‌ای که نوشته می‌شود، توضیحش فارسی است.** بدون استثنا:

- توضیح‌های داخل کد (PHP و Dart)
- نام آزمون‌ها — خودِ نام متد فارسی نوشته می‌شود
- پیام کامیت، عنوان و متن PR
- هر چیزی که در پنل یا اپ دیده می‌شود
- یادداشت‌های نسخه در `docs/CHANGELOG.fa.md`

انگلیسی فقط جایی می‌ماند که زبانِ خودِ ابزار است و عوض‌شدنی نیست: نام
متغیر و کلاس و تابع، نام ستون پایگاه داده، و کلیدهای JSON.

## نام آزمون فارسی

آزمایش شد، نه فرض: PHP حروف فارسی و حتی نیم‌فاصله را در نام متد قبول
می‌کند، و `php artisan test` همان را در خروجی نشان می‌دهد. پس
`#[TestDox]` لازم نیست — خودِ نام کافی است.

```php
public function test_گوشیِ_گزارش‌نداده_جا‌مانده_خوانده_نمی‌شود(): void
```

در Flutter همان متنِ `test('...')` فارسی نوشته می‌شود.

یک نکتهٔ عملی: `--filter` با نام فارسی در بعضی پوسته‌ها دردسر دارد.
برای اجرای یک آزمونِ تکی، راحت‌تر است کلِ فایل اجرا شود.

## توضیح‌های قدیمی

حدود ۱۴٬۵۰۰ خط توضیح انگلیسی از قبل در مخزن هست. یک‌جا ترجمه نمی‌شوند:
این توضیح‌ها استدلال دارند نه جمله، و ترجمهٔ عجولانه‌شان بدتر از
انگلیسیِ فعلی است. هر فایلی که به هر دلیلی باز و دست‌کاری می‌شود،
توضیح‌هایش هم همان‌جا فارسی می‌شوند.
